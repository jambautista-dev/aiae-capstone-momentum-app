// Shared across the Compose view and the Log page.
export const QUICK_TAGS = ["Client work", "Learning", "Collaboration", "Leadership"];

// Slalom brand rule: max 3 type sizes per screen (see branding.instructions.md).
// Canonical scale for the whole app: headline (page title only), body
// (readable content — entry text, textarea, generated reflection/coaching
// output), small (all UI chrome — labels, buttons, meta text, tags).
export const TYPE_HEADLINE = "1.7rem";
export const TYPE_BODY = "1rem";
export const TYPE_SMALL = "0.85rem";
